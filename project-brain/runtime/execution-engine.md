# Execution Engine

Guides code modifications and deployment commands securely.

## Guidelines
1. Perform local syntax checks on changed modules.
2. Confirm dependency boundaries are respected (e.g. strict import syntax for TypeScript compiler rules).
3. Do not modify files outside of classified task scope.
4. Deploy updates via CLI tools (such as InsForge deploy commands).
